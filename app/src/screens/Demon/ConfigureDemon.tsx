import React from "react";
import { View, Text, StyleSheet } from "react-native";

export const ConfigureDemon: React.FC = () => {
  return <View style={style.container}></View>;
};

const style = StyleSheet.create({
  container: {},
});
